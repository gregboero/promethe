<configuration>
    <!-- ── Console Appender (human-readable for dev) ── -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level [%thread] %logger{20} — %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ── File Appender (JSON for production/parsing) ── -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/promethe-gateway.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/promethe-gateway.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>7</maxHistory>
            <totalSizeCap>100MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{ISO8601} %-5level [%thread] %logger{36} — %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ── Framework noise control ── -->
    <logger name="io.ktor"          level="WARN"/>
    <logger name="org.eclipse.jetty" level="WARN"/>
    <logger name="org.exposed"      level="WARN"/>
    <logger name="io.netty"         level="WARN"/>
    <logger name="ai.koog"          level="INFO"/>

    <!-- ── Promethe modules ── -->
    <logger name="dev.promethe.core"    level="DEBUG"/>
    <logger name="dev.promethe.gateway" level="DEBUG"/>

    <root level="INFO">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>
